option_settings:
  # Non-secret app defaults. Secrets/region are set with `eb setenv` (see DEPLOY.md).
  aws:elasticbeanstalk:application:environment:
    ALERT_WINDOW_DAYS: "30"
    MAX_UPLOAD_MB: "20"
    PYTHONUNBUFFERED: "1"
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
